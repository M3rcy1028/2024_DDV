<!DOCTYPE html>
<html>

<head>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="/javascripts/jquery-3.7.1.min.js"></script>

    <!-- 상단바 css -->
    <link rel='stylesheet' href='/stylesheets/nav.css' />

    <!-- Bootstrap CSS -->
    <link rel='stylesheet' href='/vendors/bootstrap/css/bootstrap.min.css' />
    <link rel='stylesheet' href='/vendors/bootstrap/css/bootstrap-grid.min.css' />
    <link rel='stylesheet' href='/vendors/bootstrap/css/bootstrap-reboot.min.css' />

    <!-- Bootstrap Icon -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel='stylesheet' href='/stylesheets/rootview.css' />
    <link rel='stylesheet' href='/stylesheets/login.css' />
</head>

<body>
    <!-- 상단 바 -->
    <%- include('../nav.ejs') %>
        <div class="container">
            <h2 class="d-flex justify-content-center"><b>
                    <%= title %>
                </b></h2><br>

            <form action="/auctionBoard/auctionWrite" method="post" enctype="multipart/form-data"
                onsubmit="return onWriteSubmit()">
                <!-- <div class="form-group">
            <label style="text-align: center; margin-left: 15px; margin-bottom: 5px;" for="category"><b>카테고리</b></label>
            <select class="form-control" name="category" id="category">
                <option selected value="">카테고리 선택</option>
                <% for(var category in categories){ %>
                    <option value="<%=category%>">
                        <%= category %>
                    </option>
                    <% } %>
            </select>
        </div> -->
                <div class="form-group">
                    <label style="text-align: center; margin-left: 15px; margin-bottom: 5px;"
                        for="title"><b>제목</b></label>
                    <input style="border-radius: 10px;" class="form-control" type="text" name="title" id="title">
                </div>
                <div class="form-group">
                    <label style="text-align: center; margin-left: 15px; margin-bottom: 5px;"
                        for="content"><b>내용</b></label>
                    <textarea class="form-control" style="height: 150px;" name="content" id="content"></textarea>
                </div>
                <div class="form-group">
                    <label style="text-align: center; margin-left: 15px; margin-bottom: 5px;" for="smoney"><b>시작
                            가격</b></label>
                    <input style="border-radius: 10px;" class="form-control" type="number" name="smoney" id="smoney">
                </div>
                <div class="form-group">
                    <label style="text-align: center; margin-left: 15px; margin-bottom: 5px;" for="image"><b>상품
                            사진</b></label>
                    <input style="border-radius: 7px;" type="file" accept="image/jpeg, image/png" class="form-control"
                        name="image" id="image">
                </div>
                <!-- <div class="form-group">
            <label style="text-align: center; margin-left: 15px; margin-bottom: 5px;" for="buyDate"><b>구매 날짜</b></label>
            <input style="border-radius: 10px; text-align: center;" class="form-control" type="date" name="buyDate" id="buyDate">
        </div> -->
                <!-- <div class="form-group">
            <label style="text-align: center; margin-left: 15px; margin-bottom: 5px;" for="place"><b>거래 희망 장소</b></label>
            <input style="border-radius: 10px;" class="form-control" type="text" name="place" id="place">
        </div> -->
                <button class="btn btn3" style="width: 100%;" type="submit" id="submitBtn">등록하기</button>
            </form>
        </div>
        <!-- Bootstrap JS -->
        <script src="/vendors/bootstrap/js/bootstrap.bundle.min.js"></script>

        <script>
            function onWriteSubmit() {
                if ($("#image").val().trim() == "") {
                    var message = "상품 이미지를 선택해 주세요.";
                    $("#image").val("");
                    $("#image").focus();
                    alert(message);
                    return false;
                }
                if ($("#title").val().trim() == "") {
                    var message = "제목을 입력해 주세요.";
                    $("#title").val("");
                    $("#title").focus();
                    alert(message);
                    return false;
                }
                if ($("#smoney").val().trim() == "") {
                    var message = "시작 가격을 입력해 주세요.";
                    $("#smoney").val("");
                    $("#smoney").focus();
                    alert(message);
                    return false;
                }
                if ($("#content").val().trim() == "") {
                    var message = "내용을 입력해 주세요.";
                    $("#content").val("");
                    $("#content").focus();
                    alert(message);
                    return false;
                }
            }
        </script>
</body>

</html>